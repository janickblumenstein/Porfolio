{
  "name": "desktop",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "cv:pdf": "astro build && node scripts/generate-cv-pdf.mjs && astro build"
  },
  "dependencies": {
    "astro": "^6.2.2"
  },
  "devDependencies": {
    "playwright": "^1.58.0"
  }
}